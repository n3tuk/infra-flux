---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: runners
  namespace: app-github
spec:
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        namespace: flux-system
        name: gha-runner-scale-set
      chart: gha-runner-scale-set
      interval: 5m
      version: 0.9.3
  interval: 3h
  install:
    crds: CreateReplace
    createNamespace: false
    remediation:
      retries: 3
      remediateLastFailure: true
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
      remediateLastFailure: true
  test:
    enable: true
  driftDetection:
    mode: enabled

  dependsOn:
    - name: controllers
      namespace: app-github

  valuesFrom:
    - kind: ConfigMap
      name: helm-runners-values
    - kind: ConfigMap
      name: helm-runners-overrides
      optional: true
    - kind: Secret
      name: helm-runners-overrides
      optional: true

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: github
spec:
  sourceRef:
    kind: OCIRepository
    name: baseline
  path: github
  dependsOn:
    - name: proxmox-csi
  interval: 1h
  retryInterval: 10m
  prune: true

  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: common-substitutions
